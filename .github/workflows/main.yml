# IMPORTANT (Exam Requirement)
# Replace the contents of this file with the official workflow from:
# https://github.com/24W-GBAC/commr/blob/main/.github/workflows/main.yml
name: placeholder
on: [push]
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Replace this workflow with the professor-provided workflow file."
